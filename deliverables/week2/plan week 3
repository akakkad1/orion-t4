Largely: take the AI summary feature that already half-works in the app and turn it into something solid — define exactly what facts every vehicle summary must include, test it against messy real tracks (gaps, single pings, missing data) so it doesn't break or hallucinate, then run it across the whole saved dataset and check the results by hand. End the week with clean, organized code and a README explaining how to run it.
